import React from 'react';
import styled from 'styled-components';

const FooterPush: React.FC = () => {
    return <FooterPushContainer></FooterPushContainer>;
};

export default FooterPush;

const FooterPushContainer = styled.div`
    flex: 1;
`;
